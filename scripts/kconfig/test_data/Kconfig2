config MODULES
  bool "Enable loadable module support"
  option modules

config A
  tristate

config B
  bool "B prompt"
  select A
  default y
  select D if A
  depends on E

config C
  tristate "C prompt"
  depends on A && !B

config D
  bool
  prompt "D prompt"

config E
  bool
